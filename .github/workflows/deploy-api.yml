name: Deploy API

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "dev"
        type: choice
        options:
          - dev
          - staging
          - prod

env:
  PROJECT_ID: zagforge
  REGION: us-central1
  SERVICE: zagforge-api
  REGISTRY: us-central1-docker.pkg.dev/zagforge/zagforge

jobs:
  ci:
    name: CI Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.1"

      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest

      - name: Lint
        run: golangci-lint run ./api/... ./shared/go/...

      - name: Test
        run: go test -race ./api/... ./shared/go/...

  deploy:
    name: Build & Deploy API (${{ inputs.environment }})
    runs-on: ubuntu-latest
    needs: [ci]
    environment: ${{ inputs.environment }}
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: api/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/api:${{ github.sha }}
            ${{ env.REGISTRY }}/api:latest
          cache-from: type=gha,scope=api
          cache-to: type=gha,scope=api,mode=max
          platforms: linux/amd64

      - name: Sync Doppler secrets to Cloud Run
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_SERVICE_TOKEN }}
        run: |
          curl -sL https://cli.doppler.com/install.sh | sh
          SECRETS=$(doppler secrets download \
            --token "$DOPPLER_TOKEN" \
            --format env-no-quotes \
            --no-file \
            | grep -v '^DOPPLER_' \
            | grep -v '^PORT=' \
            | paste -sd, -)
          gcloud run services update ${{ env.SERVICE }} \
            --region ${{ env.REGION }} \
            --set-env-vars "$SECRETS" \
            --quiet

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE }}
          image: ${{ env.REGISTRY }}/api:${{ github.sha }}
          region: ${{ env.REGION }}

      - name: Run database migrations
        run: |
          gcloud run jobs execute zagforge-migrate \
            --region ${{ env.REGION }} \
            --wait \
            2>/dev/null || echo "No migrate job configured yet, skipping"
