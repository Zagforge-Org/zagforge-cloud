name: Deploy Worker

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment"
        required: true
        default: "dev"
        type: choice
        options:
          - dev
          - staging
          - prod

env:
  PROJECT_ID: zagforge
  SERVICE: zagforge-worker

jobs:
  setup:
    name: Resolve Region
    runs-on: ubuntu-latest
    outputs:
      region: ${{ steps.region.outputs.region }}
      registry: ${{ steps.region.outputs.registry }}
    steps:
      - uses: actions/checkout@v4
      - id: region
        run: |
          REGION=$(grep '^region' infra/terraform/envs/${{ inputs.environment }}.tfvars | sed 's/.*= *"\(.*\)"/\1/')
          echo "region=$REGION" >> "$GITHUB_OUTPUT"
          echo "registry=${REGION}-docker.pkg.dev/${{ env.PROJECT_ID }}/zagforge" >> "$GITHUB_OUTPUT"

  ci:
    name: CI Checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.1"

      - name: Install golangci-lint
        run: go install github.com/golangci/golangci-lint/v2/cmd/golangci-lint@latest

      - name: Lint
        run: golangci-lint run ./services/worker/... ./services/shared/go/...

      - name: Test
        run: go test -race ./services/worker/... ./services/shared/go/...

  deploy:
    name: Build & Deploy Worker (${{ inputs.environment }})
    runs-on: ubuntu-latest
    needs: [ci, setup]
    environment: ${{ inputs.environment }}
    permissions:
      id-token: write
      contents: read

    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ needs.setup.outputs.region }}-docker.pkg.dev --quiet

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: services/worker/Dockerfile
          push: true
          tags: |
            ${{ needs.setup.outputs.registry }}/worker:${{ github.sha }}
            ${{ needs.setup.outputs.registry }}/worker:latest
          cache-from: type=gha,scope=worker
          cache-to: type=gha,scope=worker,mode=max
          platforms: linux/amd64

      - name: Install Doppler CLI
        uses: dopplerhq/cli-action@v3

      - name: Sync Doppler secrets to Cloud Run
        env:
          DOPPLER_TOKEN: ${{ secrets.DOPPLER_SERVICE_TOKEN }}
        run: |
          if [ -z "$DOPPLER_TOKEN" ]; then
            echo "::error::DOPPLER_SERVICE_TOKEN secret is not set. Add it in GitHub repo settings."
            exit 1
          fi
          SECRETS=$(doppler secrets download \
            --token "$DOPPLER_TOKEN" \
            --format env-no-quotes \
            --no-file \
            | grep -v '^DOPPLER_' \
            | grep -v '^PORT=' \
            | paste -sd, -)
          if [ -z "$SECRETS" ]; then
            echo "::error::Doppler returned no secrets. Check your service token."
            exit 1
          fi
          gcloud run services update ${{ env.SERVICE }} \
            --region ${{ needs.setup.outputs.region }} \
            --set-env-vars "$SECRETS" \
            --quiet

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: ${{ env.SERVICE }}
          image: ${{ needs.setup.outputs.registry }}/worker:${{ github.sha }}
          region: ${{ needs.setup.outputs.region }}
